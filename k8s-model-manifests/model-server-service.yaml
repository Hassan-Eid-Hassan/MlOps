apiVersion: v1
kind: Service
metadata:
  name: diabetes-model-server
spec:
  selector:
    app: diabetes-model-server
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
